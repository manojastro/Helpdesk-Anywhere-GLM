{
  "name": "@helpdesk/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/test/ 2>/dev/null || echo 'no shared tests'"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
